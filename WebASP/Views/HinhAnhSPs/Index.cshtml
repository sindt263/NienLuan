@model IEnumerable<WebASP.Models.HinhAnhSP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SanPham.SerialNumber)
        </th>
        <th>
            <b>Link</b>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SanPham.SerialNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LinkHASP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHASP }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHASP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHASP })
        </td>
    </tr>
}

</table>
